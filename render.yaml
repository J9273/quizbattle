services:
  - type: web
    name: quizbattle
    runtime: php
    plan: free
    buildCommand: composer install --no-dev --optimize-autoloader
    startCommand: php -S 0.0.0.0:$PORT -t .
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: APP_ENV
        value: production